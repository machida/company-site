= form_for [:staffs, @snippet] do |f|
  - if @snippet.errors.any?
    #error_explanation
      %h2
        = pluralize(@snippet.errors.count, "error")
        prohibited this snippet from being saved:
      %ul
        - @snippet.errors.full_messages.each do |msg|
          %li= msg
  .field
    = f.label :title, t('Title')
    %br/
    = f.text_field :title
  .field
    = f.label :body, t('Body')
    %br/
    = f.text_area :body
  .field
    = f.label :slug, t('Slug')
    %br/
    = f.text_field :slug
  .actions
    = f.submit(t('Update'))
